<h1>Edit!!</h1>
<form action="/figures/<%=@figure.id%>" method="post">
  <input type="hidden" id="hidden" name="_method" value="patch">

<h2>Edit:</h2>
  <input type="text" name="figure[name]" id="figure_name" value="<%= @figure.name %>">

<h2>Create or Edit Existing Title:</h2>

<h4>Create:</h4>
<input type="text" name="title[name]" id="new_title">

<h4>Edit:</h4>
  <% Title.all.each do |title|%>
    <input type="checkbox" name="figure[title_ids][]" value="<%= title.id %>" id="title_<%= title.id %>"
    <%='checked' if @figure.titles.include?(title) %>>
    <%= title.name %>
  <%end%>


<h2>Create or Edit Existing Landmark:</h2>
<h4>Create:</h4>
    <input type="text" name="landmark[name]]" id="new_landmark">

<h4>Edit:</h4>
  <% Landmark.all.each do |landmark| %>
    <input type="checkbox" name="figure[landmark_ids][]" value="<%= landmark.id %>" id="landmark_<%= landmark.id %>"
    <%='checked' if @figure.landmarks.include?(landmark) %>>
    <%= landmark.name %>
  <% end %>


    <button><input type="submit" value="Edit Figure"></button>
</form>
<%
